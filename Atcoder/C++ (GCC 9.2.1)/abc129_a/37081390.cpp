/// #RM
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main()
{
  vector<int >r;
  //set<int>::iterator it;
  int a,b,c; cin >> a >> b >> c; r.push_back(a); r.push_back(b); r.push_back(c); sort(r.begin(),r.end());
  cout << r[0] + r[1] << '\n';



}
