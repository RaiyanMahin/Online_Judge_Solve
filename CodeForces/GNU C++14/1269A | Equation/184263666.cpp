#include "bits/stdc++.h"
using ll = long long int;
using namespace std;
int main()
{
    ll m; cin >> m;
    if(m == 1) cout << 9 << ' ' << 8 << '\n';
    else cout << (9 * m) << ' ' <<  (8*m) << '\n';
}