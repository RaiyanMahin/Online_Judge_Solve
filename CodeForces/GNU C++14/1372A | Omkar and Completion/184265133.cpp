#include <bits/stdc++.h>
using ll = long long;
using namespace std;
#define debug(x) cout << " --- >" << x << '\n'
int main()
{
    ll t; cin >> t;
    while(t--)
    {
        ll n; cin >> n;
        vector < ll > b(n , 1);
        for(auto i : b)
        {
            cout << i << ' ';
            
        }
        cout << '\n';
        b.clear();
 
 
    }
}