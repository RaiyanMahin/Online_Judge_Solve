#include <bits/stdc++.h>
using namespace std;
using ll =  long long;
int main()
{
    ll t; cin >> t;
    while(t--)
    {
        ll n, k; cin >> n >> k;
        if(n == 1)
            cout << 0 << '\n';
        if(n == 2)
            cout << k << '\n';
        if(n > 2)
        {
            cout << 2 * k << '\n';
        }
           
 
 
 
 
        }
}
 