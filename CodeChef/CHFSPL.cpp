#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define debug(x) cerr << # x << ' ' << x << endl;

void baal()
{
    ll a, b, c; cin >> a >> b >> c;
   ll p = a + b, q = b + c, r = a + c;
   cout << max( { p ,q, r } ) << endl;











    return;









}

int main()
{
    ll t; cin >> t;
    while(t--)
        baal();
}

